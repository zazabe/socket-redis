#!/usr/bin/env node

var redisIo = require('../redis.io.js');
var worker = new redisIo.worker(process.argv[2]);

process.on('message', function(message) {
	if (message.channelId && message.message) {
		worker.publish(message.channelId, message.message);
	}
});